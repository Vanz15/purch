# Secrets / local env — never commit
.env
.env.*
!.env.example
*.local

# OLD read-only reference checkout — do not commit (it's a nested git clone)
OLD/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node / Next.js (frontend has its own .gitignore already)
frontend/node_modules/
